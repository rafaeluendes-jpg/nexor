node_modules/

# MAPA.md e gerado do index.html de hoje (node ferramentas/mapear.js).
# Um mapa commitado apontaria as linhas de duas versoes atras, e linha
# errada e pior do que mapa nenhum.
MAPA.md

# copias do banco: nunca no repositorio
backup/

# pasta do site publicado: gerada pelo enxugador na hora de publicar
_site/

# snapshot do Redis, escrito na pasta de onde o servidor sobe:
# e estado de fila, descartavel, nunca conteudo do projeto
dump.rdb
